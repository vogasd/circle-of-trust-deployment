apiVersion: v1
kind: Service
metadata:
  name: circle-ollama
  namespace: circle-prod
  labels:
    app: circle-ollama
    component: llm
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http
      port: 11434
      targetPort: http
      protocol: TCP
  selector:
    app: circle-ollama
